# Optimized Route Finder (Streamlit)

A working prototype to find optimized routes between multiple locations using OpenRouteService API.

## Setup

1. **Install dependencies**
```bash
pip install -r requirements.txt
